pipeline {
	agent {
    		dockerfile {
			filename '04 - Dockerfile - Multiple Agents using Dockerfile'
			dir '003 - Defining Execution Environments'
			additionalBuildArgs ''
			args ''
		}
	}
    stages {
        stage('maven') {
            steps {
                sh "mvn --version"
            }
        }
	stage('node') {
            steps {
                sh "npm --version"
		003 - Defining Execution Environments/04 - Dockerfile - Multiple Agents using Dockerfile-01
            }
        }
    }
}
