pipeline {
    agent { 
   	dockerfile {
            dir ''
            additionalBuildArgs ''
            args ''
        }
	}
    stages {
        stage('build') {
            steps {
                sh 'if [ "$ENV_VARIABLE" != "Value for execution" ]; then exit 1; fi'
            }
        }
    }
}
