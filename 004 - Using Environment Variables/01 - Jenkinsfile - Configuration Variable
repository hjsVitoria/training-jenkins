pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
	environment{
		NOTIFICATION-EMAIL='BATATAS'
	}
    stages {
        stage('build') {
            steps {
                echo "${NOTIFICATION_EMAIL}"
            }
        }
    }
}
