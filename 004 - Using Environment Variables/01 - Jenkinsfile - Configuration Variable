pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
	environment{
		NOTIFICATION-EMAIL='EMAIL'
	}
    stages {
        stage('build') {
            steps {
                echo "$NOTIFICATION_EMAIL"
            }
        }
    }
}
