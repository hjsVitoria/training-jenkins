pipeline {
	agent {
		docker { 
			image 'alpine:latest' 
		}
	}
	environment{
		NOTIFICATION_EMAIL='BATATAS'
	}
    stages {
        stage('build') {
            steps {
                echo "${NOTIFICATION_EMAIL}"
            }
        }
    }
}
