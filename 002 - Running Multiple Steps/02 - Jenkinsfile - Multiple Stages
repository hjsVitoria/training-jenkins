pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
    	stage('build'){
		steps{
			sh 'date > date.txt'
		}
	}
	stage('test'){
		sh 'grep 2019 date.txt'
	}
    }
}
