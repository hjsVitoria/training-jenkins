pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
	parameters{
		string(name: 'PERSON', defaultValue: 'Mr Jenkins', description: 'Qual stage?')
	}
    stages {
	    stage('build'){
	    }
	    stage('test'){
	    }
    }
}
