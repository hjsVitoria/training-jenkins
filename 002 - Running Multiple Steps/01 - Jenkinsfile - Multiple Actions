pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
    	stage('get-data'){
		steps{
			script{
			
				saveAndPrintDate('date')
			}
		}
	}
    }
}
def saveAndPrintDate(filename = '') {
    	sh 'date > $filename && cat $filename'
}
